<table>
  <tr>
    <th>ID</th>
    <th>Содержание</th>
    <th colspan='3'>Действия</th>
    <th><%= link_to 'Новый тест', new_test_path %></th>
  </tr>
  <% resource&.each do |r| %>
  <tr>
    <td><%= r.id %></td>
    <td><%= r.title %></td>
    <td><%= link_to 'Корректировать', edit_test_path(r) %></td>
    <td><%= link_to 'Подробнее', test_path(r) %></td>
    <td><%= button_to 'Удалить', test_path(r), 
      method: :delete, 
      data: { confirm: 'Точно удаляем?' }, onclick: "return confirm('Точно удаляем?');" %></td>
  </tr>
  <% end %>
</table>